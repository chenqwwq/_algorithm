#include <stdc++.h>
#include <common.h>

class Solution {
public:
    TreeNode *increasingBST(TreeNode *root) {
        
    }

    void inorder(TreeNode *node,vector<int> $ans){
        if(node == nullptr){
            return;
        }
        inorder(node->left,ans);
        res.push
    }
};