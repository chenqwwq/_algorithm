//
// Created by chenqwwq on 2022/1/7.
//

#include "stdc++.h"
#include "common.h"

using namespace std;

class Dichotomy {

public:
    // 二分的本质并不一定是单调性
    // 可以二分的不一定具有单调性，但是有单调性的肯定可以二分
    // 二分的本质是区间内的边界，区间内可以找到一个点，使得区间可以一分为二，一边满足一边不满足(所以单调性完美满足这个点，
    // 根据这个特定每次计算可以去除一半的

    // l = mid,需要是 l + r + 1 >> 1（向下取整导致




};

